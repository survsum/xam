import React from 'react';

export default function BlurredTileField({ theme, mousePos }) {
  // Completely clear tile field component - returning null to keep center clean & dark canvas
  return null;
}
